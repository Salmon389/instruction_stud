![sapmle image](Git-logo-black.svg.png)


git это сохранение версии

git -- version - эта команда позволяет посмотреть версию гита

git init активирует гит

git status проверяет статус файлов

git add выбераем фаел какой сохранить

git commit -m "x" сохранение файла

git log проверяем все сохраненые версии

git checkout[] выбераем сохранение 

git diff Показывать изменения между коммитами, фиксацией и рабочим деревом и т.д

git что то